import { NEW_MESSAGE } from "../../../constants/text";

export const NewMessages = () => {
  return (
    <div className="flex justify-center py-4 bg-blue-light text-blue text-xs">
      {NEW_MESSAGE}
    </div>
  );
};
